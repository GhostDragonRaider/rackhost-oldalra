# Másold ezt a blokkot a Nginx default config-ba, a location / ELÉ.
# Így a /projects/ alatti kérések közvetlenül a fájlrendszerből szolgálódnak ki.

location /projects/ {
    alias /var/www/html/projects/;
}
